<%= form_with(model: ability) do |form| %>
  <% if ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ability.errors.count, "error") %> prohibited this ability from being saved:</h2>

      <ul>
        <% ability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_main_series %>
    <%= form.text_field :is_main_series %>
  </div>

  <div class="field">
    <%= form.label :bool %>
    <%= form.text_field :bool %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
